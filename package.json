{
  "name": "test-helpers",
  "version": "0.1.0",
  "description": "Some utils making your testing routine simpler",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "test": "echo `no tests available`",
    "fast-clean-install": "rm -rf ./node_modules && npm install",
    "prepublishOnly": "npm run build",
    "postpublish": "rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Mtnt/testHelpers.git"
  },
  "keywords": [
    "test",
    "helper",
    "utility",
    "utils"
  ],
  "author": "Kirill \"Mutant\" Glazunov <reg@webmutant.ru>",
  "license": "MIT",
  "dependencies": {
    "es6-error": "^4.0.2",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
